<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Un mensaje especial bajo las estrellas" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>Para ti ✨</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    margin: 0;
    width: 100%;
    min-height: 100vh;
    overflow-x: hidden;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
    background: #000;
    color: #fff;
  }

  /* Scrollbar personalizado bonito y rosadito */
  /* Para navegadores WebKit (Chrome, Safari, Edge) */
  ::-webkit-scrollbar {
    width: 12px;
  }

  ::-webkit-scrollbar-track {
    background: rgba(10, 10, 26, 0.8);
    border-radius: 10px;
    margin: 5px;
  }

  ::-webkit-scrollbar-thumb {
    background: linear-gradient(180deg, #ff6b9d 0%, #ffa6c1 50%, #ff8fb3 100%);
    border-radius: 10px;
    border: 2px solid rgba(10, 10, 26, 0.8);
    box-shadow: 0 0 10px rgba(255, 182, 193, 0.5);
  }

  ::-webkit-scrollbar-thumb:hover {
    background: linear-gradient(180deg, #ff5a8c 0%, #ff95b0 50%, #ff7ea2 100%);
    box-shadow: 0 0 15px rgba(255, 182, 193, 0.8);
  }

  ::-webkit-scrollbar-thumb:active {
    background: linear-gradient(180deg, #ff4975 0%, #ff84a0 50%, #ff6d91 100%);
  }

  /* Para Firefox */
  * {
    scrollbar-width: thin;
    scrollbar-color: #ff8fb3 rgba(10, 10, 26, 0.8);
  }
</style>
